imm-dokuwiki-fork

A fork of the imm Dokuwiki template by Ian McIntosh (http://ianmcintosh.org/wiki/template).

This fork adds (or will add) the following (among other things):
    * A sidebar
    * More attractive interface elements
